node_modules/
dist/
dist-electron/
*.dll
*.zip
*.app

# Tauri build output
src-tauri/target/

# Backend binaries (rebuilt by scripts/build-backend.js)
src-tauri/binaries/

# Electron
node_modules/electron/

# Secrets — never commit. Use Render/host env vars instead. (.env.example is fine.)
.env
.env.local
.env.*.local
